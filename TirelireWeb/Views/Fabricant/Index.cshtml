<!--access model with lowercase-->
@model List<Fabricant>

<div class="container">
    <div class="row pt-4 pb-3">
        <div class="col-6">
            <h2 class="text-primary">
                Liste des Fabricants
            </h2>
        </div>
        <div class="col-6 text-end">
            <a asp-controller="Fabricant" asp-action="Create" class="btn btn-dark">
                <i class="bi bi-plus-square"></i> Ajouter un fabricant
            </a>
        </div>
    </div>
    <table class="table table-bordered table-striped">
        <thead>
            <tr>
                <th>
                    Nom du fabricant
                </th>
                <th>
                    Description
                </th>
                <th>Pays</th>
            </tr>
        </thead>
        <tbody>
            <!--retrieve data from model with Uppercase "Model"-->
            @foreach (var obj in Model)
            {
                <tr>
                    <td>@obj.Name</td>
                    <td>
                        @obj.Description
                    </td>
                    <td>@obj.Pays</td>
                    <td>
                        <div class="w-75 btn-group" role="group">
                            <a asp-controller="Fabricant" asp-action="Edit" asp-route-id="@obj.Id" class="btn btn-primary mx-2">
                                <i class="bi bi-pencil-square"></i> Edit
                            </a>
                            <a asp-controller="Fabricant" asp-action="Delete" asp-route-id="@obj.Id" class="btn btn-danger mx-2">
                                <i class="bi bi-trash3"></i> Delete
                            </a>
                        </div>
                    </td>
                </tr>
            }
        </tbody>

    </table>
</div>
